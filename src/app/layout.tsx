import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ScrollArcade — TikTok for Mini Games",
  description:
    "Swipe through endless bite-sized interactive mini-games and experiences. The TikTok of web games.",
  keywords: ["mini games", "arcade", "scroll", "web games", "fun"],
  openGraph: {
    title: "ScrollArcade",
    description: "TikTok-style infinite scroll of mini-games",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
